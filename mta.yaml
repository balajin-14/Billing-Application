_schema-version: 3.3.0
ID: BillingApp
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: BillingApp-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: BillingApp-db
      - name: BillingApp-auth
      - name: BillingApp-destination

  - name: BillingApp-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: BillingApp-db
  - name: BillingApp
    type: approuter.nodejs
    path: app/router
    parameters:
      keep-existing-routes: true
      disk-quota: 256M
      memory: 256M
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true
      - name: BillingApp-auth
      - name: BillingApp-destination
      - name: BillingApp-html5-runtime
    provides:
      - name: app-api
        properties:
          app-protocol: ${protocol}
          app-uri: ${default-uri}

  - name: BillingApp-app-deployer
    type: com.sap.application.content
    path: gen
    requires:
      - name: BillingApp-auth
      - name: srv-api
      - name: BillingApp-html5-repo-host
        parameters:
          content-target: true
          config:
            HTML5Runtime_enabled: true
    build-parameters:
      build-result: app/
      requires:
        - name: BillingAppbilling
          artifacts:
            - billing.zip
          target-path: app/

  - name: BillingAppbilling
    type: html5
    path: app/billing
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []

  - name: BillingApp-destinations
    type: com.sap.application.content
    requires:
      - name: BillingApp-auth
        parameters:
          service-key:
            name: BillingApp-auth-key
      - name: BillingApp-html5-repo-host
        parameters:
          service-key:
            name: BillingApp-html5-repo-host-key
      - name: srv-api
      - name: BillingApp-destination
        parameters:
          content-target: true
    build-parameters:
      no-source: true
    parameters:
      content:
        instance:
          existing_destinations_policy: update
          destinations:
            - Name: BillingApp-html5-repository
              ServiceInstanceName: BillingApp-html5-repo-host
              ServiceKeyName: BillingApp-html5-repo-host-key
              sap.cloud.service: BillingApp.service
            - Name: BillingApp-auth
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: BillingApp-auth
              ServiceKeyName: BillingApp-auth-key
              sap.cloud.service: BillingApp.service

resources:
  - name: BillingApp-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: BillingApp-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: BillingApp-${org}-${space}
        tenant-mode: dedicated
        oauth2-configuration:
          credential-types:
            - "binding-secret"
            - "x509"
          redirect-uris:
            - https://*~{app-api/app-uri}/**
        role-collections:
          - name: 'BillingUser (BillingApp ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.BillingUser'
    requires:
      - name: app-api
  - name: BillingApp-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - Name: srv-api
                URL: ~{srv-api/srv-url}
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                HTML5.ForwardAuthToken: true
                HTML5.DynamicDestination: true
    requires:
      - name: srv-api

  - name: BillingApp-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
  - name: BillingApp-html5-runtime
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-runtime
